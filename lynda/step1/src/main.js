// var $ = require('jquery');

// in your modules just require the stylesheet
// This has the side effect that a <style>-tag is added to the DOM.
// require('./style.css');
require('./sass/sass_style.scss');

// $('#target').html('hello the world');